
oind_PROGRAMS = liboindirect.so
oinddir = /lib
liboindirect_so_SOURCES = liboindirect.c
liboindirect_so_CFLAGS = -D_GNU_SOURCE -fPIC -Wall -pthread -shared
liboindirect_so_CPPFLAGS = -D_FILE_OFFSET_BITS=64 -D_GNU_SOURCE
liboindirect_so_LDFLAGS = -module -avoidversion -shared -ldl

CLEANFILES =
